#main.pure-g
  #lspacing.pure-u-md-1-8
  #ldash.pure-u-1.pure-u-md-1-6
    #{partial(:profile, :locals => {:userid => profileinfo["uid"], :avatar_url => profileinfo["avatarurl"], :username => profileinfo["name"], :handle => profileinfo["handle"], :followingcount => profileinfo["followingcount"], :user_relation => profileinfo["user_relation"], :followercount => profileinfo["followercount"]})}
    #{partial(:bleatcomposebox)}
  .pure-u-md-1-24
  #timeline.pure-u-md-13-24.pure-u-1
    #{partial(:bleat, :collection => bleats)}
  #rspacing.pure-u-md-1-8
